# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec sakura
bindsym $mod+Return exec sakura

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -nb "#000000" -nf "#ffffff" -sb "#000000" -sf "#0c6235"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace 1:Web
bindsym $mod+2 workspace 2:Sage_Cloud
bindsym $mod+3 workspace 3:Sage
bindsym $mod+4 workspace 4:Files
bindsym $mod+5 workspace 5:Reading
bindsym $mod+6 workspace 6:LaTeX
bindsym $mod+7 workspace 7:Image
bindsym $mod+8 workspace 8:Audio
bindsym $mod+9 workspace 9:Video
bindsym $mod+0 workspace 10:etc

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:Web
bindsym $mod+Shift+2 move container to workspace 2:Sage_Cloud
bindsym $mod+Shift+3 move container to workspace 3:Sage
bindsym $mod+Shift+4 move container to workspace 4:Files
bindsym $mod+Shift+5 move container to workspace 5:Reading
bindsym $mod+Shift+6 move container to workspace 6:LaTeX
bindsym $mod+Shift+7 move container to workspace 7:Image
bindsym $mod+Shift+8 move container to workspace 8:Audio
bindsym $mod+Shift+9 move container to workspace 9:Video
bindsym $mod+Shift+0 move container to workspace 10:etc

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
bindsym $mod+t border toggle
#bindcode 107 exec --no-startup-id import -window root screenshot.jpg
bindcode 107 exec --no-startup-id scrot

bindsym $mod+Shift+o exec --no-startup-id killall -SIGUSR1 dunst # pause
bindsym $mod+Shift+p exec --no-startup-id killall -SIGUSR2 dunst # resume 

bindsym Mod1+r exec --no-startup-id reboot
bindsym Mod1+s exec --no-startup-id shutdown now

bindsym $mod+Mod1+1 exec --no-startup-id udevil mount /dev/sdb1
bindsym $mod+Mod1+Shift+1 exec --no-startup-id udevil umount /dev/sdb1

bindsym $mod+Mod1+t exec --no-startup-id sh googletranslate
bindsym $mod+Mod1+y exec --no-startup-id sh yandexlingvo
bindsym $mod+Mod1+g exec --no-startup-id sh google

new_window pixel 2
new_float pixel 2

popup_during_fullscreen smart

# class                 border  backgr. text    indicator
client.focused          #000000 #000000 #ffffff #ffffff
client.focused_inactive #000000 #000000 #ffffff #ffffff
client.unfocused        #000000 #2e3436 #ffffff #ffffff
client.urgent           #1f4874 #1f4874 #0c6235 #1f4874

# workspace names
workspace "5:Reading" output VGA1
workspace "1:Web" output VGA1
workspace "2:Sage_Cloud" output VGA1
workspace "3:Sage" output VGA1
workspace "4:Files" output VGA1
workspace "6:LaTeX" output VGA1
workspace "7:Image" output VGA1 
workspace "9:Video" output VGA1
workspace "8:Audio" output VGA1
workspace "10:etc" output VGA1


# assign zathura to workspace 5:Reading
# assign [class="^Zathura$"] 5:Reading
assign [class="^Mplayer$"] 9:Video
assign [class="^Deadbeef$"] 8:Audio
assign [class="^Gimp$"] 7:Image
# assign [class="^Gvim$"] 6:LaTeX
# assign [class="^Microsoft$"] 6:LaTeX

exec --no-startup-id feh --bg-scale /home/andreyzh/Downloads/wall1.jpg
exec --no-startup-id setxkbmap -layout "us,ru" -option "grp:shift_caps_toggle"
exec --no-startup-id perWindowLayoutD
#exec --no-startup-id owncloud
exec --no-startup-id dunst

# exec --no-startup-id i3-msg 'workspace 10:etc; exec "sakura -x CopyConsole"'

# autostart 
# exec --no-startup-id i3-msg 'workspace 1:Web; exec firefox'
# exec --no-startup-id i3-msg 'workspace 2:Sage_Cloud; exec dwb -n cloud.sagemath.com'
# exec --no-startup-id i3-msg 'workspace 3:Sage; exec dwb -n localhost:8080'
# exec --no-startup-id i3-msg 'workspace 10:etc; exec "sakura -x ~/autosage"'
# exec --no-startup-id i3-msg 'workspace 10:etc; exec "sakura -x ~/autoipy"'
# exec --no-startup-id i3-msg 'workspace 4:Files; exec "sakura -x ranger"'

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	position top
        status_command i3status
	mode hide
	hidden_state hide
	modifier Mod4
	
	colors {
        	background #000000
	        statusline #ffffff

        	focused_workspace  #ffffff #000000
	        active_workspace   #ffffff #000000
	        inactive_workspace #ffffff #000000
	        urgent_workspace   #ffffff #000000
    }
}
